--- a/extension.js
@@ -240,9 +240,11 @@
     if (this._magnifier && this._magnifier.handleKeyEvent(event)) {
       return Clutter.EVENT_STOP;
     }
+    if (this._spotlight) {
+      this._spotlight.handleKeyEvent(event);
+    }
     return Clutter.EVENT_PROPAGATE;
   }
-
   _tick() {
     if (!this._containerActor || !this._highlightActor)
       return Clutter.TICK_STOP;
@@ -275,6 +277,16 @@
       this._spotlight.update(curX, curY);
     }
 
+    if (this._spotlight) {
+      const active = this._spotlight.isActive();
+      this._containerActor.remove_all_transitions();
+      this._containerActor.opacity = active ? 255 : 0;
+      this._containerActor.set_scale(1.0, 1.0);
+      if (active) {
+        this._canvas.queue_repaint();
+      }
+    }
+
     if (!this._clickState) {
       this._clickState = {
         active: false,
@@ -406,10 +418,13 @@
     this._canvas.set_size(totalSize, totalSize);
     this._canvas.queue_repaint();
 
-    this._containerActor.set_opacity(255);
+    this._containerActor.set_opacity(0);
   }
 
   _drawHighlight(area) {
+    if (!this._spotlight || !this._spotlight.isActive()) {
+      return;
+    }
     renderHighlight(area, {
       drawSettings: this._drawSettings,
       clickState: this._clickState,
--- a/modules/spotlight.js
@@ -1,6 +1,7 @@
 // SPDX-License-Identifier: GPL-3.0-or-later
 
 import Clutter from "gi://Clutter";
+import GLib from "gi://GLib";
 import St from "gi://St";
 import * as Main from "resource:///org/gnome/shell/ui/main.js";
 import { getShapeValue } from "../utils.js";
@@ -17,6 +18,8 @@
     this._rotation = settings.get_int("rotation");
     this._cornerRadius = settings.get_int("corner-radius");
     this._active = false;
+    this._prevPressed = false;
+    this._lastPressTime = 0;
     this._actor = null;
     this._effect = null;
     this._x = 0;
@@ -89,6 +92,10 @@
     }
   }
 
+  isActive() {
+    return this._active;
+  }
+
   _updateUniforms() {
     if (!this._effect) return;
 
@@ -123,13 +130,35 @@
     };
 
     const targetMask = keyMap[this._key];
+    if (targetMask === undefined) return;
+
     const isPressed = (mask & targetMask) !== 0;
+    const now = GLib.get_monotonic_time() / 1000; // ms
+    const doublePressWindow = 500; // ms: ventana para considerar doble pulsacion
 
-    if (isPressed && !this._active) {
-      this.activate();
-    } else if (!isPressed && this._active) {
-      this.deactivate();
+    if (isPressed && !this._prevPressed) {
+      // flanco ascendente: nueva pulsacion de la tecla
+      if (
+        this._lastPressTime !== 0 &&
+        now - this._lastPressTime <= doublePressWindow
+      ) {
+        // doble pulsacion detectada: alternar spotlight
+        if (this._active) {
+          this.deactivate();
+        } else {
+          this.activate();
+        }
+        this._lastPressTime = 0;
+      } else {
+        this._lastPressTime = now;
+      }
     }
+
+    this._prevPressed = isPressed;
+  }
+
+  handleKeyEvent(event) {
+    return false;
   }
 
   activate() {
@@ -157,8 +186,8 @@
 
     this._updateUniforms();
 
-    global.stage.add_child(this._actor);
-    global.stage.set_child_above_sibling(this._actor, null);
+    Main.uiGroup.add_child(this._actor);
+    Main.uiGroup.set_child_above_sibling(this._actor, null);
 
     this._actor.ease({
       opacity: 255,
--- a/modules/auto-hide.js
@@ -77,7 +77,7 @@
     this._stationaryTime = 0;
     if (this._containerActor) {
       this._containerActor.remove_all_transitions();
-      this._containerActor.opacity = 255;
+      this._containerActor.opacity = 0;
       this._containerActor.set_scale(1.0, 1.0);
     }
   }
@@ -106,7 +106,7 @@
     this._containerActor.set_pivot_point(0.5, 0.5);
 
     this._containerActor.ease({
-      opacity: 255,
+      opacity: 0,
       scale_x: 1.0,
       scale_y: 1.0,
       duration: 150,
