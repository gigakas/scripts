# Copia este archivo a .env
# Opcional: LLM para el resumen. Si no hay key, el grafo degrada a resumen heurístico.
OPENAI_API_KEY=
*** base_url alternativo: OpenRouter, Groq, Ollama (http://localhost:11434/v1), etc.
LLM_BASE_URL=
LLM_MODEL=gpt-4o-mini
